const { Scenes } = require("telegraf");
const { settingsMenu } = require("./../../assets/func/components");
const { model } = require("../../assets/db/models/user");

exports.settings = async (tg) => {

    

    return scene;

}